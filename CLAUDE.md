# 旅行计划项目约定

## 项目目标

维护个人旅行计划。Excel 是唯一可信数据源，通过生成脚本自动构建适合 iPhone 浏览和离线使用的 PWA 并自动发布；HTML 不作为反向编辑入口。

## 文件与 Sheet 组织

- 主工作簿：[travel.xlsx](travel.xlsx)。
- 一个 Excel 文件保存多次旅行。
- 每次旅行独立一个 Sheet。
- 同一次旅行不拆成“行程、预算、交通、住宿”等多个 Sheet。
- 每个旅行 Sheet 都以执行时间为主线，自上而下排列交通、住宿、游览、餐饮等事项。
- 可以增加全局索引 Sheet，但不是必需项。
- “26暑期云南”是当前默认发布页。
- “模板验证-清明重庆”是新版模板的验证页；“26清明重庆成都”是原始内容页，未经明确要求不要覆盖或删除。

## 仓库与发布

- 生成器位于 `scripts/generate_pwa.py`，默认读取“26暑期云南”Sheet，输出到被 Git 忽略的 `output/`。
- GitHub Actions 工作流位于 `.github/workflows/deploy.yml`；推送 `travel.xlsx`、生成器、依赖或工作流变更到 `main` 后自动构建并发布。
- 发布目标与仓库解耦，由 GitHub 仓库变量 `DEPLOY_TARGET` 控制，当前默认 `github-pages`：`github-pages`（GitHub Pages）、`tencent`（腾讯云 CloudBase 静态托管，需按量计费环境）或 `none`（仅构建不发布）。
- 腾讯云部署所需凭证存放在 GitHub Actions Secrets，不写进仓库：`TENCENT_SECRET_ID`、`TENCENT_SECRET_KEY`（CAM 密钥）、`TENCENT_ENV_ID`（CloudBase 环境 ID）。
- 正式访问地址为 <https://hellsge.github.io/travel-plan/>，不保留局域网服务器、临时预览脚本或重复的生成文档。
- 发布产物包括 `index.html`、`manifest.webmanifest`、`service-worker.js` 和 `icons/`，均由脚本生成，不提交、不手工编辑。
- 修改行程后应提交 Excel 并推送，由 CI 自动发布；发布后验证线上页面、PWA 资源及 GitHub Actions 状态。
- 仓库本地提交身份使用 `hellsge <hellsge@qq.com>`。

## 新版模板结构

当前模板列定义：

| 列 | 字段 | 约定 |
| --- | --- | --- |
| A | 日期 | 使用真实 Excel 日期值 |
| B | 开始 | 使用真实 Excel 时间值，显示为 `hh:mm` |
| C | 结束 | 使用真实 Excel 时间值，显示为 `hh:mm` |
| D | 类型 | 使用标准类型和固定单元格底色 |
| E | 项目 | 行程事项名称 |
| F | 路线 / 备注 | 路线、地址、营业时间等补充信息 |
| G | 单价 | 数值，货币格式 |
| H | 数量 | 数值 |
| I | 消费 | 优先使用 `单价 × 数量` 公式；无法推导时可填实际总价 |
| J | 开售 / 预约时间 | 使用真实 Excel 日期时间值 |
| K | 状态 | 使用标准状态和固定视觉规则 |
| L | 地点 / 地图关键词 | 可被地图准确识别的 POI；为空时网页不显示地图操作 |

## 时间轴和日期分隔

- 时间轴是模板核心，不要按活动类别拆散行程。
- 每天可以保留一行合并单元格作为醒目的日期/城市分隔。
- 具体事项行仍须在 A 列填写真实日期，不能只依赖分隔标题。
- B、C 列提供 `00:00` 至 `23:50`、间隔 10 分钟的下拉选项。
- 时间下拉只是便捷输入，必须允许手工输入车次、航班等精确时间，例如 `19:28`。
- 时间选项可以保存在隐藏辅助列中，不影响打印和浏览。

## 顶部摘要区

- B2 放开始日期，C2 放结束日期。
- B2、C2 使用正常字体，不使用缩小字体来容纳长文本。
- 不要为了顶部摘要拉宽 B、C 时间列。
- 顶部保留预计总消费和待办数量。

## 类型规范

标准类型：

- 交通
- 住宿
- 游览
- 餐饮
- 日常
- 购物
- 其他

D 列使用 Excel 原生“单元格值等于”条件格式自动设置明显底色，不对整行按类型着色：

- 交通：蓝色
- 住宿：紫色
- 游览：绿色
- 餐饮：橙色
- 日常：灰色
- 购物：粉色
- 其他：棕灰色

类型文字使用深色粗体。通过下拉框修改类型后，底色应自动变化，不需要手工填色。创建或修改条件格式后，必须关闭并重新打开 Excel，通过 `DisplayFormat.Interior.Color` 验证实际显示效果，不能只检查规则是否存在。

## 状态规范

标准状态：

- 无需预订
- 待购买
- 待预约
- 已购买
- 已预约
- 已完成
- 已取消

K 列使用 Excel 原生“单元格值等于”条件格式，根据下拉选择自动设置单元格自身底色；不改变整行底色，也不主要依赖文本颜色区分：

- 待购买、待预约：黄色
- 已购买、已预约、已完成：绿色
- 无需预订：浅蓝灰色
- 已取消：灰色

状态文字统一使用深色粗体。通过下拉框修改状态后，底色应自动变化，不需要手工填色。顶部待办数量只改变摘要单元格自身底色。

条件格式应优先使用 Excel 原生 `Cell Value = 文本` 规则。当前环境中，部分由 `openpyxl` 写入的公式型条件格式可能存在但无法渲染；必要时通过 Excel COM 创建规则。写入 COM 颜色值时必须先完整计算颜色整数再赋给 `Interior.Color`，避免规则保存为无填充色。修改后必须关闭并重新打开工作簿，再检查 `DisplayFormat.Interior.Color` 并实际切换一次下拉值验证动态变色。

## 视觉和使用原则

- 日期分隔行使用蓝色体系，保持时间轴层级清晰。
- 类型颜色只作用于 D 列。
- 状态颜色只作用于 K 列。
- 不使用待办整行着色，避免页面过花以及覆盖类型颜色。
- 冻结标题行并启用筛选。
- 隐藏辅助数据列，不纳入打印区域。
- 保持适合横向打印和手机端 HTML 转换的紧凑列宽。
- 不删除有实际意义的未定事项；可以清理只有 `0`、空值或默认未勾选符号的纯占位行。

## 公式与数据维护

- Excel 是唯一可信数据源，不手工维护一份内容重复的 HTML。
- 消费可以由单价和数量确定时，I 列使用公式，不复制静态结果。
- 日期、时间、金额必须保存为对应的数据类型，不保存为仅供显示的字符串。
- 下拉选项用于规范输入，但不能阻止合理的精确时间输入。
- 修改模板时尽量保留原始旅行 Sheet，以新 Sheet 验证后再决定是否迁移。

## PWA 生成与移动端规范

PWA 从指定旅行 Sheet 自动生成，并沿用 Excel 的类型和状态语义。当前功能包括：

- 手机友好的按天时间轴和日期快捷导航；
- 默认突出当天或最近一天行程；
- 仅对独立地点字段提供高德、Apple、百度地图选择，并记住上次选择；
- 地点、完整事项复制等快捷操作；
- 待购买、待预约事项提示；
- 顶部浅色/深色模式切换，记忆选择；
- 当前/下一事项标记、离线状态和新版本刷新提示；
- 当日及全程预算汇总；
- Service Worker 离线缓存和主屏幕安装。

移动端以 iPhone 16（`393 × 852`）和 iPhone 16 Pro（`402 × 874`）为主要验证尺寸：

- 保留 `viewport-fit=cover`，并使用 `env(safe-area-inset-*)` 适配灵动岛、状态栏和 Home Indicator。
- 关键触控区域至少 `44px`，避免横向溢出和 Safari 自动文字缩放。
- 同时验证 Safari 浏览器和主屏幕 `standalone` 模式；视觉效果不能只凭构建成功判断，应按目标尺寸截图检查。
- 页面主体支持离线使用；地图等外部服务仍需网络。

PWA 是派生文件。Excel 内容变化后应重新生成并发布，避免双向维护造成数据不一致。
